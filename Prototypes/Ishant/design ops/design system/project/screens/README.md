# Working screens

Place migrated HTML/CSS/JS screens here. This directory is served at `/screens/`.
Import shared implementations from `/design-system/`; do not depend on `/demo/`.
Keep the untouched supplied source in ../inbox/ for comparison.
