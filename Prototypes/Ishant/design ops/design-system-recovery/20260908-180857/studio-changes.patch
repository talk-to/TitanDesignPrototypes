diff --git a/CHANGELOG.md b/CHANGELOG.md
index a86dfb9..40ce78d 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,16 @@
 # Changelog
 
+## Unreleased
+
+- Use the full content workspace for selected sections; keep the introduction only on Overview and remove the repeated source-screen header.
+
+- Replace the workbench presentation with a compact dark sidebar and catalog overview.
+- Add nested foundation categories, alias-aware token previews, search and real counts.
+- Keep source screens and existing component previews accessible through the sidebar.
+- Add token parsing and navigation regression tests. Catalog contract v1 is unchanged.
+- Local UI development only: framework version/lock are not regenerated for a release.
+
+
 ## 0.2.0 — plug-in studio and app-owned content
 
 - Initialize against existing apps without an inbox, screen copying or relocation.
diff --git a/framework/workbench/index.html b/framework/workbench/index.html
index 520e535..9a4e1e8 100644
--- a/framework/workbench/index.html
+++ b/framework/workbench/index.html
@@ -1,6 +1,6 @@
 <!doctype html>
-<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Design System Studio</title><link rel="stylesheet" href="/framework/workbench/studio.css"><script defer src="/framework/workbench/studio.js"></script></head>
-<body>
-<aside class="rail"><a class="brand" href="#overview"><span class="brand-mark">D</span>Design System<br><small>STUDIO / 01</small></a><label class="profile-label" for="profile">Working catalog</label><select id="profile"><option value="demo">Inspector sandbox</option><option value="project">Your project</option></select><nav aria-label="Workbench"></nav><div class="rail-footer"><span class="live-dot"></span> Local development<br><small id="version"></small></div></aside>
-<main><header class="topbar"><span id="catalog-name">Loading catalog…</span><a id="sandbox-link" href="/demo/screens/record.html?ds=true" target="_blank" rel="noopener">Open inspector playground ↗</a></header><div class="page-head"><p class="eyebrow" id="eyebrow">WORKBENCH</p><h1 id="title">Overview</h1><p id="intro"></p></div><div id="content" aria-live="polite"></div></main>
+<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Design System</title><link rel="stylesheet" href="/framework/workbench/studio.css"><script defer src="/framework/workbench/tokens.js"></script><script defer src="/framework/workbench/studio.js"></script></head>
+<body><a class="skip-link" href="#main">Skip to content</a>
+<aside class="rail"><a class="brand" href="#overview"><strong id="brand-name">Studio</strong><span>DESIGN SYSTEM</span></a><nav id="catalog-nav" aria-label="Design system"></nav><div class="rail-footer"><div id="catalog-summary"></div><label class="profile-label" for="profile">Catalog</label><select id="profile"><option value="demo">Inspector sandbox</option><option value="project">Your project</option></select><small id="version"></small></div></aside>
+<main id="main" tabindex="-1"><div class="page-head" id="page-head"><p class="eyebrow" id="eyebrow">DESIGN SYSTEM</p><h1 id="title">Overview</h1><p id="intro"></p></div><div id="content" aria-live="polite"></div></main>
 </body></html>
diff --git a/framework/workbench/studio.css b/framework/workbench/studio.css
index a14e59b..9bc4a55 100644
--- a/framework/workbench/studio.css
+++ b/framework/workbench/studio.css
@@ -1 +1,13 @@
 :root{font:14px/1.55 system-ui,-apple-system,sans-serif;color:#26302d;background:#f6f7f5;--line:#e0e5df;--ink:#1e2925;--muted:#66736c;--green:#315e4a}*{box-sizing:border-box}body{margin:0;display:grid;grid-template-columns:240px 1fr;min-height:100vh}a{color:var(--green)}button,select,input{font:inherit}button,select{cursor:pointer}button:focus-visible,a:focus-visible,select:focus-visible,input:focus-visible{outline:3px solid #5b8fd3;outline-offset:3px}.rail{padding:32px 20px;background:#eef1eb;border-right:1px solid var(--line);height:100vh;position:sticky;top:0;display:flex;flex-direction:column}.brand{text-decoration:none;color:var(--ink);font-size:19px;font-weight:650;line-height:1.2;display:block;padding:0 12px 34px}.brand-mark{float:left;margin-right:12px;background:#315e4a;color:white;padding:9px 12px;border-radius:10px;font-size:22px}.brand small{font-size:9px;letter-spacing:2px;color:var(--muted);font-weight:500}.profile-label{color:var(--muted);font-size:11px;margin:0 10px 6px}.rail select{width:100%;padding:10px;border:1px solid #d0d8cf;border-radius:8px;background:#fafbf9;color:var(--ink);margin-bottom:28px}.rail nav{display:grid;gap:5px}.rail nav a{display:flex;align-items:center;gap:12px;text-decoration:none;color:#5a685f;padding:10px 13px;border-radius:8px}.rail nav a[aria-current=page]{background:#dfe7dc;color:#244c39;font-weight:650}.nav-num{font-size:10px;opacity:.6;font-variant-numeric:tabular-nums}.rail-footer{margin-top:auto;padding:24px 12px 0;color:var(--muted);font-size:12px}.rail-footer small{margin-left:15px}.live-dot{display:inline-block;background:#57825e;width:6px;height:6px;border-radius:50%;margin-right:6px}main{min-width:0;padding:0 46px 64px}.topbar{height:76px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);font-size:12px;gap:16px}.topbar a{text-decoration:none}.page-head{max-width:760px;padding:44px 0 30px}.eyebrow{font-size:10px;font-weight:650;letter-spacing:2px;color:var(--green);margin:0 0 10px}h1{font-size:38px;line-height:1.2;letter-spacing:-1.1px;margin:0 0 12px;font-weight:600}h2{font-size:18px;margin:0 0 8px}h3{font-size:15px;margin:0 0 8px}.page-head>p:last-child{color:var(--muted);max-width:670px;margin:0}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(360px,100%),1fr));gap:20px}.card{background:white;border:1px solid var(--line);border-radius:12px;padding:24px;min-width:0}.card p{color:var(--muted);margin:8px 0 16px}.card-head{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;margin-bottom:18px}.tag{font-size:10px;letter-spacing:.5px;white-space:nowrap;padding:4px 8px;border-radius:5px;background:#eef3eb;color:#416247}.metric-row{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:24px}.metric{padding:22px;background:white;border:1px solid var(--line);border-radius:10px}.metric strong{font-size:29px;font-weight:550;display:block}.metric span{font-size:12px;color:var(--muted)}.preview{width:100%;height:300px;border:1px solid var(--line);border-radius:8px;background:#fff;display:block}.preview.tall{height:600px}.actions{display:flex;flex-wrap:wrap;gap:12px;margin-top:15px;font-size:12px}.actions button{background:white;border:1px solid var(--line);border-radius:6px;padding:6px 10px;color:var(--green)}details{margin-top:18px;font-size:12px}summary{cursor:pointer;color:var(--muted)}pre{white-space:pre-wrap;overflow-wrap:anywhere;font-size:11px;background:#f5f7f3;padding:14px;border-radius:6px}input[type=search]{width:100%;border:1px solid var(--line);border-radius:8px;padding:12px;margin-bottom:20px;background:white}.token-table{border-collapse:collapse;width:100%;font-size:12px}.token-table td,.token-table th{padding:12px;text-align:left;border-bottom:1px solid var(--line);overflow-wrap:anywhere}.token-table code{font-size:11px}.token-table td:last-child{color:var(--muted)}.empty{padding:40px;border:1px dashed #c5d1c2;border-radius:12px;background:#f0f4ec}.empty p{max-width:600px;color:var(--muted)}.notice{padding:14px 18px;background:#eaf0e4;border-radius:8px;margin-bottom:24px;color:#4a6245;font-size:12px}.error{background:#fff0ef;color:#8c332b;padding:24px;border-radius:8px}.source-link{font-size:12px}.full-frame{width:100%;height:72vh;border:1px solid var(--line);border-radius:12px}.section-space{margin-top:28px}.width-tools{display:flex;gap:6px;margin-bottom:10px}.width-tools button{border:1px solid var(--line);border-radius:5px;background:#fff;padding:4px 8px;font-size:11px}@media(max-width:900px){body{grid-template-columns:190px 1fr}main{padding:0 24px 40px}.rail{padding:24px 12px}.metric-row{grid-template-columns:repeat(2,1fr)}}@media(max-width:640px){body{display:block}.rail{position:static;height:auto;padding:16px}.brand{padding-bottom:18px}.rail nav{display:flex;overflow:auto}.rail nav a{white-space:nowrap}.rail-footer,.profile-label{display:none}.rail select{margin-bottom:12px}.topbar{height:auto;min-height:65px;flex-wrap:wrap;padding:14px 0}main{padding:0 18px 36px}.page-head{padding-top:28px}h1{font-size:30px}}
+/* Catalog navigation: persistent rail, compact content, isolated from app tokens. */
+:root{font:14px/1.5 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#292929;background:#f5f5f8;--line:#e6e6eb;--ink:#242424;--muted:#85858b;--green:#2170f4}
+body{grid-template-columns:232px minmax(0,1fr)}[hidden]{display:none!important}.skip-link{position:fixed;top:-80px;left:250px;z-index:50;padding:10px 16px;background:white}.skip-link:focus{top:12px}
+.rail{background:#1d1d1d;border-right:1px solid #303030;padding:24px 10px 16px;height:100dvh;overflow-y:auto}.brand{display:flex;flex-wrap:wrap;align-items:baseline;gap:6px;padding:0 8px 26px;font-size:15px;line-height:1.6;color:#eee}.brand strong{font-weight:600;max-width:100%;overflow-wrap:anywhere}.brand>span{font:9px ui-monospace,monospace;letter-spacing:1px;color:#969696}.rail nav{gap:2px}.rail nav>a{font-size:13px;padding:9px 10px;gap:9px;min-height:36px;border-radius:5px;color:#aaa}.rail nav a:hover{background:#282828;color:#eee}.rail nav a[aria-current=page]{background:#333;color:#f4f4f4;font-weight:500}.nav-icon{width:16px;text-align:center;flex-shrink:0;font-size:17px;color:#999;line-height:1}.rail nav a small{margin-left:auto;color:#777;font-size:11px;font-variant-numeric:tabular-nums}.subnav{display:grid;margin:4px 0 8px 27px;border-left:1px solid #3b3b3b;padding-left:6px}.rail .subnav a{display:flex;font-size:12px;gap:8px;padding:7px 10px;color:#999;text-decoration:none;border-radius:4px}.rail .subnav a[aria-current=page]{color:#fff;background:#292929}.rail-footer{border-top:1px solid #303030;padding:18px 8px 0;margin-top:auto;color:#919191;font:11px/1.8 ui-monospace,monospace}.profile-label{display:block;color:#777;margin:14px 0 5px;font:10px -apple-system,sans-serif}.rail select{background:#252525;color:#b4b4b4;border:1px solid #3a3a3a;border-radius:4px;padding:6px 8px;margin:0 0 10px;font-size:11px}.rail-footer small{font-size:10px;margin-left:0;color:#747474}
+main{width:100%;max-width:1240px;margin:0 auto;padding:0 56px 56px;outline:none}.topbar{height:56px;font-size:11px;color:#919197;border:0}.topbar a{color:#73737b}.topbar a:hover{color:#2170f4}.page-head{max-width:820px;padding:22px 0 26px}.eyebrow{color:#909097;font:10px ui-monospace,monospace;letter-spacing:1.2px;margin-bottom:12px}h1{font-size:28px;letter-spacing:-.8px;font-weight:650;margin-bottom:12px}h2{font-size:16px;font-weight:600}.page-head>p:last-child{color:#808087;font-size:14px;line-height:1.65;max-width:670px}.metric-row{grid-template-columns:repeat(3,minmax(0,1fr));gap:0;margin:0 0 28px;border:1px solid var(--line);border-radius:7px;background:white;overflow:hidden}.metric{border:0;border-right:1px solid var(--line);border-radius:0;padding:18px 20px}.metric:last-child{border-right:0}.metric strong{font-size:30px;font-weight:500;line-height:1.15;margin-bottom:6px;letter-spacing:-1px}.metric span{font-size:12px;color:#999;line-height:1.5;display:block}.catalog-table-wrap,.token-table-wrap{border:1px solid var(--line);border-radius:7px;background:#fff;overflow:auto}.section-heading{display:flex;justify-content:space-between;align-items:center;gap:16px;padding:16px 18px}.section-heading h2{margin:0;font-size:14px}.section-heading>span{color:#aaa;font:10px ui-monospace,monospace}.catalog-table{width:100%;border-collapse:collapse;text-align:left;font-size:13px}.catalog-table thead{background:#fafafd;color:#999;font:10px ui-monospace,monospace;letter-spacing:.5px}.catalog-table th,.catalog-table td{padding:14px 18px;border-top:1px solid #f0f0f4}.catalog-table tbody th{font-weight:500}.catalog-table th a{text-decoration:none;color:#333;display:flex;align-items:center;gap:14px}.catalog-table th a:hover{color:#2170f4}.catalog-table td{color:#999;font-size:12px}.catalog-table td:last-child,.catalog-table th:last-child{text-align:right;font-variant-numeric:tabular-nums}.status-dot{width:7px;height:7px;flex-shrink:0;border-radius:50%;background:#32a557}.status-dot.pending{background:#c7c7cc}.footnote{font-size:11px;line-height:1.6;color:#999;margin:14px 0 24px}.review-notes{border-top:1px solid var(--line);padding:18px 0;font-size:12px;color:#888}.review-notes summary{color:#777}.review-notes summary span{margin-left:8px;color:#aaa}.review-notes li{margin:8px 0;max-width:800px}.token-tools{display:flex;align-items:center;gap:20px;margin-bottom:18px}.token-tools input[type=search]{max-width:480px;margin:0;padding:10px 12px;border-radius:5px;font-size:12px}.token-tools>span{margin-left:auto;white-space:nowrap;font-size:11px;color:#999}.token-table{font-size:12px;min-width:650px}.token-table thead{background:#fafafd}.token-table thead th{font:10px ui-monospace,monospace;color:#999;letter-spacing:.4px}.token-table th,.token-table td{padding:14px 16px;border-bottom:1px solid #f0f0f4;text-align:left;vertical-align:middle}.token-table tbody th{font-weight:400;min-width:200px}.token-table tbody th small{display:block;color:#a0a0a7;font:10px/1.6 ui-monospace,monospace;margin-top:4px}.token-table tbody tr:last-child>*{border-bottom:0}.token-table code{font-size:11px;overflow-wrap:anywhere}.token-table td:last-child a{font:10px ui-monospace,monospace;color:#909097;text-decoration:none}.token-table td:last-child a:hover{text-decoration:underline;color:#2170f4}.token-value{max-width:250px;color:#777}.sample-cell{width:90px}.token-sample{display:block;font-size:18px;color:#444;line-height:1.2}.token-sample[data-kind=colors]{width:40px;height:26px;border-radius:4px;border:1px solid #00000012;background-image:none}.token-sample[data-kind=spacing]{height:8px;background:#b9cff5;border-radius:2px}.no-results{padding:24px;color:#999;text-align:center}.card{border-radius:7px;padding:20px}.card-head h2{font-size:15px}.tag{background:#f3f4f8;color:#777;font-size:9px}.preview{border-radius:4px}.empty{border:1px solid var(--line);border-radius:7px;background:white;padding:42px}.empty-mark{display:block;color:#aaa;font-size:28px;margin-bottom:12px}.empty h2{font-size:17px}.empty p{font-size:13px;color:#999}.empty a{font-size:12px;text-decoration:none}.notice{background:#f0f1f5;color:#888;border-radius:5px}.grid{gap:18px}.full-frame{border-radius:6px}pre{background:#f7f7fa}.width-tools button{border-radius:4px}
+@media(max-width:1100px){main{padding:0 28px 40px}body{grid-template-columns:212px minmax(0,1fr)}}
+@media(max-width:760px){body{grid-template-columns:180px minmax(0,1fr)}.rail{padding:20px 8px}.brand{gap:2px}.brand>span{font-size:8px}.rail nav>a{font-size:12px;gap:6px;padding:8px}.rail .subnav a{font-size:11px;padding:7px 5px}main{padding:0 20px 32px}.metric-row{grid-template-columns:1fr}.metric{border-right:0;border-bottom:1px solid var(--line);padding:14px 16px}.metric:last-child{border-bottom:0}.metric strong{font-size:25px}h1{font-size:25px}.catalog-table{min-width:430px}.page-head{padding-top:18px}}
+@media(max-width:640px){body{display:grid;grid-template-columns:156px minmax(0,1fr)}.rail{position:sticky;height:100dvh;top:0}.brand{padding:0 6px 20px}.rail nav{display:grid;overflow:visible}.rail nav>a{white-space:normal}.rail-footer,.profile-label{display:block}.rail select{width:100%}.subnav{margin-left:15px}.topbar{min-height:56px;padding:12px 0;font-size:10px}.topbar>span{display:none}.metric-row{margin-bottom:18px}.token-tools{flex-wrap:wrap;gap:8px}.token-tools input[type=search]{min-width:0}.token-tools>span{margin:0}main{padding:0 14px 28px}.empty{padding:20px}h1{font-size:22px}.page-head>p:last-child{font-size:12px}.skip-link{left:166px}}
+
+/* Selected sections use the complete workspace; only Overview has an introduction. */
+body{height:100dvh;min-height:0;overflow:hidden}main{max-width:none;margin:0;padding:24px 28px;height:100dvh;overflow:auto;display:flex;flex-direction:column}.rail{min-height:0}.page-head{padding:8px 0 24px}.view-content #content{display:flex;flex-direction:column;flex:1;min-height:0}.view-content .empty{flex:1;display:flex;flex-direction:column;justify-content:center;align-items:center;text-align:center}.view-content .token-table-wrap{flex:1;min-height:0;overflow:auto}.view-content .token-tools,.view-content .footnote{flex-shrink:0}.token-table thead{position:sticky;top:0;z-index:1}.view-content .full-frame{flex:1;min-height:300px;height:auto}.view-content .grid{width:100%}.view-content .notice{margin-bottom:16px}.view-overview #content{width:100%}
+@media(max-width:760px){main{padding:18px}}@media(max-width:640px){main{padding:12px}.page-head{padding:8px 0 18px}}
diff --git a/framework/workbench/studio.js b/framework/workbench/studio.js
index 4440b99..36b2ad3 100644
--- a/framework/workbench/studio.js
+++ b/framework/workbench/studio.js
@@ -1,9 +1,10 @@
 (() => {
   'use strict';
-  const tabs=[['overview','Overview','A place to define your design language, inspect real screens, and evolve the tools that keep them connected.'],['foundations','Foundations','Scales, semantic roles and component controls, read directly from the token source.'],['icons','Icons','Discover symbols through the project’s icon provider.'],['components','Components','Real implementations, anatomy, optional parts and states.'],['patterns','Patterns','Contracts for how tasks occupy a screen.'],['visualizations','Data visualization','Chart language, reading order and renderer examples.'],['directions','Directions','The same examples rendered with different semantic themes.'],['moodboard','Moodboard','Collect references and capture the decisions they inform.']];
+  const tabs=[['overview','Overview','A place to define your design language, inspect real screens, and evolve the tools that keep them connected.'],['foundations','Foundations','Scales, semantic roles and component controls, read directly from the token source.'],['icons','Icons','Discover symbols through the project’s icon provider.'],['components','Components','Real implementations, anatomy, optional parts and states.'],['patterns','Patterns','Contracts for how tasks occupy a screen.'],['visualizations','Data visualization','Chart language, reading order and renderer examples.'],['directions','Directions','The same examples rendered with different semantic themes.'],['screens','Screens','Existing app screens and registered foundation specimens.'],['moodboard','Moodboard','Collect references and capture the decisions they inform.']];
   const $=id=>document.getElementById(id);
   const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
-  let registry,profile,base,renderId=0;
+  let registry,profile,base,tokens=[],renderId=0;
+  const groups=[['all','All foundations'],['colors','Colors'],['typography','Typography'],['spacing','Spacing'],['shape','Shape & elevation'],['motion','Motion'],['other','Other']];
   const source=relative=>new URL(relative,new URL(base,location.origin)).href;
   function safeURL(relative){const url=new URL(source(relative));if(url.origin!==location.origin)throw Error('Catalog URLs must be same-origin');return url;}
   function previewURL(entry,theme,inspect=false){const url=safeURL(entry.preview);if(theme)url.searchParams.set('theme',theme);if(inspect)url.searchParams.set('ds','true');return url.href;}
@@ -12,21 +13,38 @@
     const url=previewURL(entry,theme);
     return `<article class="card"><div class="card-head"><div><h2>${esc(entry.name)}</h2>${entry.class?`<code>${esc(entry.class)}</code>`:''}</div><span class="tag">${esc(entry.category||'CONTRACT')}</span></div>${entry.usage?`<p>${esc(entry.usage)}</p>`:''}<div class="width-tools" role="group" aria-label="Preview width"><button data-width="100%">Fluid</button><button data-width="320px">320px</button></div>${frame(url,entry.name)}<div class="actions"><a href="${esc(previewURL(entry,theme,true))}" target="_blank" rel="noopener">Inspect example ↗</a>${entry.css?`<a href="${esc(source(entry.css))}" target="_blank" rel="noopener">CSS source</a>`:''}${entry.module?`<a href="${esc(source(entry.module))}" target="_blank" rel="noopener">Renderer</a>`:''}</div><details><summary>Contract &amp; anatomy</summary><pre>${esc(JSON.stringify(entry,null,2))}</pre></details></article>`;
   }
-  function empty(title){return `<div class="empty"><h2>${esc(title)}</h2><p>Point your agent at existing screens wherever they live. Follow the attached content folder’s AGENTS.md to extract and register this app’s tokens, components and previews. No screen copying or inbox is required.</p><p>Not attached yet? Run <code>node studio.js init --app /path/to/app</code>, then start with the generated <code>--config</code> path.</p><a href="?profile=demo#overview">Explore the populated sandbox ↗</a></div>`;}
+  function empty(title){return `<div class="empty"><span class="empty-mark" aria-hidden="true">○</span><h2>${esc(title)}</h2><p>This section will fill as the design system grows. Existing foundations and screens are available from the sidebar.</p><a href="#foundations">Browse foundations →</a></div>`;}
+  function count(tab){if(tab==='foundations')return tokens.length;if(tab==='components')return registry.components.length+registry.compositions.length;if(tab==='directions')return registry.themes.length;return registry[tab]?.length;}
+  function navigation(active,group){
+    const symbols={overview:'▤',foundations:'◇',icons:'⊞',components:'▣',patterns:'⌘',visualizations:'⌁',directions:'◐',screens:'▱',moodboard:'⊟'};
+    $('catalog-nav').innerHTML=tabs.map(t=>`<a href="#${t[0]}" ${active===t[0]?'aria-current="page"':''}><span class="nav-icon" aria-hidden="true">${symbols[t[0]]}</span><span>${esc(t[1])}</span>${count(t[0])!==undefined?`<small>${count(t[0])}</small>`:''}</a>${t[0]==='foundations'&&active==='foundations'?`<div class="subnav">${groups.filter(g=>g[0]==='all'||tokens.some(t=>t.group===g[0])).map(g=>`<a href="#foundations/${g[0]}" ${group===g[0]?'aria-current="page"':''}>${g[1]}<small>${g[0]==='all'?tokens.length:tokens.filter(t=>t.group===g[0]).length}</small></a>`).join('')}</div>`:''}`).join('');
+  }
+  function overview(){
+    const items=groups.filter(g=>g[0]!=='all'&&tokens.some(t=>t.group===g[0]));
+    return `<div class="metric-row">${[[tokens.length,'Foundation declarations'],[registry.components.length+registry.compositions.length,'Components & compositions'],[registry.screens.length,'Registered previews']].map(([n,label])=>`<div class="metric"><strong>${n}</strong><span>${label}</span></div>`).join('')}</div><section class="catalog-table-wrap"><div class="section-heading"><h2>Explore the system</h2><span>Current catalog</span></div><table class="catalog-table"><thead><tr><th>Section</th><th>What’s inside</th><th>Entries</th></tr></thead><tbody>${items.map(g=>`<tr><th><a href="#foundations/${g[0]}"><span class="status-dot"></span>${g[1]}</a></th><td>Foundation tokens</td><td>${tokens.filter(t=>t.group===g[0]).length}</td></tr>`).join('')}${[['components','Components'],['icons','Icons'],['patterns','Patterns'],['screens','Screens']].map(([id,label])=>`<tr><th><a href="#${id}"><span class="status-dot ${count(id)?'':'pending'}"></span>${label}</a></th><td>${count(id)?'Registered in the catalog':'Not extracted yet'}</td><td>${count(id)}</td></tr>`).join('')}</tbody></table></section><p class="footnote">Counts reflect catalog entries and token declarations, not product adoption or verified states.</p>${registry.knownGaps?.length?`<details class="review-notes"><summary>Review notes <span>${registry.knownGaps.length}</span></summary><ul>${registry.knownGaps.map(g=>`<li>${esc(g)}</li>`).join('')}</ul></details>`:''}`;
+  }
+  function foundations(group){
+    const rows=group==='all'?tokens:tokens.filter(t=>t.group===group);
+    if(!rows.length)return empty('No foundations in this section yet');
+    return `<div class="token-tools"><input type="search" aria-label="Filter tokens" placeholder="Search names, values or sources…" id="token-filter"><span id="result-count">${rows.length} entries</span></div><div class="token-table-wrap"><table class="token-table"><thead><tr><th>Preview</th><th>Token</th><th>Value</th><th>Source</th></tr></thead><tbody>${rows.map(r=>`<tr><td class="sample-cell"><span class="token-sample" data-kind="${r.group}" data-value="${esc(r.resolved)}" data-name="${esc(r.name)}">${r.group==='typography'?'Aa':r.group==='colors'?'':r.group==='spacing'?'':'—'}</span></td><th scope="row"><code>${esc(r.name)}</code>${r.value!==r.resolved?`<small>${esc(r.value)}</small>`:''}</th><td class="token-value"><code>${esc(r.resolved)}</code></td><td><a href="${esc(source(r.file))}" target="_blank" rel="noopener">${esc(r.file)}</a></td></tr>`).join('')}</tbody></table><p class="no-results" hidden>No matching tokens.</p></div><p class="footnote">Values come from the token source. Local or theme overrides in a screen may differ.</p>`;
+  }
   async function render(){
     if(!registry)return;
-    const id=++renderId,tab=tabs.find(t=>t[0]===location.hash.slice(1))||tabs[0];
+    const id=++renderId,[route,requestedGroup]=location.hash.slice(1).split('/'),tab=tabs.find(t=>t[0]===route)||tabs[0];
+    const group=groups.some(g=>g[0]===requestedGroup)?requestedGroup:'all';
     $('title').textContent=tab[1];$('intro').textContent=tab[2];$('eyebrow').textContent=(profile==='demo'?'TOOL DEVELOPMENT':'PROJECT DESIGN SYSTEM')+' / '+tab[1].toUpperCase();
-    document.querySelector('nav').innerHTML=tabs.map((t,i)=>`<a href="#${t[0]}" ${tab===t?'aria-current="page"':''}><span class="nav-num">0${i+1}</span>${t[1]}</a>`).join('');
+    $('page-head').hidden=tab[0]!=='overview';
+    $('main').className=tab[0]==='overview'?'view-overview':'view-content';
+    $('main').ariaLabel=tab[0]==='foundations'&&group!=='all'?groups.find(g=>g[0]===group)[1]:tab[1];
+    navigation(tab[0],group);
+    if(tab[0]==='foundations'&&group!=='all')$('title').textContent=groups.find(g=>g[0]===group)[1];
     let html='';
     if(tab[0]==='overview'){
-      html=`<div class="notice">${profile==='demo'?'Sandbox data is fictional. This catalog is for developing the framework; it never populates your project automatically.':'Project files belong to you. Framework releases update the tooling independently.'}</div><div class="metric-row">${[['components','Components'],['compositions','Compositions'],['patterns','Patterns'],['screens','Screens']].map(([key,label])=>`<div class="metric"><strong>${registry[key].length}</strong><span>${label}</span></div>`).join('')}</div><div class="grid">${registry.screens.map(entry=>card(entry)).join('')||empty('Ready for your first screens')}</div>`;
-      if(registry.knownGaps?.length)html+=`<article class="card section-space"><h2>Known gaps</h2><ul>${registry.knownGaps.map(g=>`<li>${esc(g)}</li>`).join('')}</ul></article>`;
+      $('title').textContent='Your design system, in one place.';
+      $('intro').textContent='Browse the foundations, explore real screens, and grow the catalog one considered step at a time.';
+      html=overview();
     }else if(tab[0]==='foundations'){
-      const files=await Promise.all(registry.tokenFiles.map(async file=>({file,text:await (await fetch(source(file))).text()})));
-      if(id!==renderId)return;
-      const rows=files.flatMap(({file,text})=>Array.from(text.replace(/\/\*[\s\S]*?\*\//g,'').matchAll(/(--[\w-]+)\s*:\s*([^;{}]+);/g),m=>({name:m[1],value:m[2],file})));
-      html=rows.length?`<input type="search" aria-label="Filter tokens" placeholder="Find a token, value or role…" id="token-filter"><div class="card"><table class="token-table"><thead><tr><th>Token</th><th>Definition</th><th>Source</th></tr></thead><tbody>${rows.map(r=>`<tr><td><code>${esc(r.name)}</code></td><td>${esc(r.value)}</td><td><a href="${esc(source(r.file))}">${esc(r.file)}</a></td></tr>`).join('')}</tbody></table></div>`:empty('No foundations extracted yet');
+      html=foundations(group);
     }else if(tab[0]==='moodboard'){
       html='<div class="notice">References belong to the attached app’s content folder (or the standalone sandbox). Studio updates do not replace them. Profiles in this session share the board.</div>'+frame('/moodboard/','Reference moodboard','full-frame');
     }else if(tab[0]==='directions'){
@@ -37,7 +55,20 @@
       html=entries.length?`<div class="grid">${entries.map(entry=>card(entry)).join('')}</div>`:empty('No '+tab[1].toLowerCase()+' registered yet');
     }
     if(id!==renderId)return;$('content').innerHTML=html;
-    $('token-filter')?.addEventListener('input',event=>document.querySelectorAll('.token-table tbody tr').forEach(row=>row.hidden=!row.textContent.toLowerCase().includes(event.target.value.toLowerCase())));
+    $('token-filter')?.addEventListener('input',event=>{
+      let visible=0;document.querySelectorAll('.token-table tbody tr').forEach(row=>{row.hidden=!row.textContent.toLowerCase().includes(event.target.value.toLowerCase());if(!row.hidden)visible++;});
+      $('result-count').textContent=visible+' entries';document.querySelector('.no-results').hidden=visible!==0;
+    });
+    document.querySelectorAll('.token-sample').forEach(sample=>{
+      const {kind,value,name}=sample.dataset;
+      if(kind==='colors'&&CSS.supports('background-color',value))sample.style.backgroundColor=value;
+      if(kind==='spacing'&&/^\d+(?:\.\d+)?px$/.test(value))sample.style.width=Math.min(parseFloat(value),80)+'px';
+      if(kind==='typography'){
+        if(/family|font-system/.test(name)&&CSS.supports('font-family',value))sample.style.fontFamily=value;
+        else if(/weight/.test(name)&&CSS.supports('font-weight',value))sample.style.fontWeight=value;
+        else if(/size/.test(name)&&/^\d+(?:\.\d+)?px$/.test(value))sample.style.fontSize=Math.min(parseFloat(value),32)+'px';
+      }
+    });
     document.querySelectorAll('[data-width]').forEach(button=>button.onclick=()=>{const iframe=button.closest('.card').querySelector('iframe');iframe.style.width=button.dataset.width;iframe.style.maxWidth='100%';});
   }
   async function load(){
@@ -47,12 +78,12 @@
       if(!['demo','project'].includes(profile))profile='project';
       base=profile==='demo'?'/demo/design-system/':'/design-system/';
       registry=await (await fetch(source('registry.json'))).json();
-      $('profile').value=profile;$('catalog-name').textContent=registry.name;$('version').textContent='Framework '+config.version.version;
-      if(config.attachment){
-        const notice=document.createElement('p');notice.className='notice';
-        notice.textContent='App-owned content: '+config.attachment.content+' · Screens stay in '+config.attachment.appRoot;
-        document.querySelector('.page-head').append(notice);
-      }
+      const files=await Promise.all(registry.tokenFiles.map(async file=>{const response=await fetch(source(file));if(!response.ok)throw Error('Could not load '+file);return {file,text:await response.text()};}));
+      tokens=studioTokens.parse(files);
+      $('brand-name').textContent=registry.name.replace(/\s*design system$/i,'').trim()||'Studio';
+      document.title=registry.name;
+      $('catalog-summary').textContent=tokens.length+' foundations · '+(registry.components.length+registry.compositions.length)+' components';
+      $('profile').value=profile;$('version').textContent='Framework '+config.version.version;
       $('profile').onchange=()=>{const url=new URL(location.href);url.searchParams.set('profile',$('profile').value);location.href=url.href;};
       await render();
     }catch(error){$('content').innerHTML='<div class="error">Could not load the catalog: '+esc(error.message)+'</div>';}
