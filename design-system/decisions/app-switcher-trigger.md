# App-switcher trigger

Observed icon variant of Dropdown trigger: 28px app image, 16px chevron slot,
9×5px chevron, 2px inset and 4px radius. Dark-context hover retained. No split
action model or menu content added. DS shell uses native button with shared CSS;
existing menu click hooks retained and aria-expanded synchronized. Original shell
untouched. Menu layout remains separate. Preview, states and four insets registered.
Browser verification pending; component and attachment checks run.
