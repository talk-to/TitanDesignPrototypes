# Composer extraction

Source: DS-specific TitanEmailShell's New email window (former inline composer markup/CSS and openComposer, closeComposer, toggleComposerMinimize functions). Original Shells/TitanEmailShell is unchanged.

Registered Composer field row, Formatting toolbar and Email composer. Shared actions provide compact composer Button, 28px composer and 24px inverse window Icon button, 32px Send Split button. Dropdown trigger supplies a borderless composer variant. These are child controls, not duplicate component families. Gallery and interaction previews expose the new child appearances. Parent sidebar links dependencies instead of repeating child states. Field row owns native editing/focus. Toolbar operations, sending, feature actions and menu contents remain caller callbacks; source had no implemented operations. Existing source open/close/minimize and To autofocus remain connected to the shared renderer.

Semantic --titan-composer-content-inset aliases 16px and aligns header, fields, features, editor and send actions. Existing compact icon-label 4px role is reused. 6px formatting gaps and 14px toolbar inset remain component-scoped, not universal semantics. Header/feature/send action gaps use primitive 8px; trailing Cc/Bcc use 12px. Registry records padding/gap/separator margins. Controls own their internal sizing; parent owns external space. Composer preferred dimensions 681x640, header44, formatbar48; main editor flexes and scrolls. Horizontal toolbar scrolling keeps narrow arrangements reachable. Viewport max width/height bounds are added for containment, not a claim of a designed mobile layout.

The field row unifies sender/recipient/subject structure with intrinsic labels and flexible input; source-specific fixed label widths are not promoted to global semantics. Row separators use existing observed literal colors; source's 2px separators remain distinct from 1px region boundaries. The composer window now consumes the source-observed `--titan-elevation-composer-window` role; elevation remains window-owned and does not establish a global numeric scale. Source artwork is retained in composer-assets; live controls use these real assets.

Bottom-docked composer is an Interaction Pattern with a low-fidelity minimize/restore diagram. No expanded state is claimed: source Expand is not wired. Focus return, recipient completion, Cc/Bcc expansion, formatting and send operations need actual product implementation. Native button semantics replace source clickable divs/spans; no real email is sent by the specimen.

Verification: shared renderer escaping, required labels, preserved action set, asset resolution, independent split callbacks, minimize/close and listener cleanup tested. Attachment validation and DS tests run. Browser visual, keyboard, clipboard and computed-spacing verification remain pending because a browser surface is unavailable. No visual approval is claimed.

SVG sizing correction: composer-owned SVG copies now use xMidYMid meet, preventing non-square artwork from stretching inside square slots. Original shell exports are unchanged. Reminder/formatting 32px canvases and Drive/Bookings 24px canvases preserve their built-in whitespace while matching the visual weight of tightly cropped 19px glyphs. Button hit areas remain unchanged. These optical corrections belong to composer presentation, not shared spacing semantics.

Docked motion is now shared in patterns/bottom-docked-panel.css: 300ms cubic-bezier(.16,1,.3,1), translateY entry/exit and header-only minimization, reduced-motion immediate transitions. This is a newly approved motion contract. The DS-specific composer and low-fidelity pattern preview both import it.
