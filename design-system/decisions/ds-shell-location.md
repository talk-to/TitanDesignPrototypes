# DS-specific shell location

Preserved the current DS-connected email shell and every asset under
`Prototypes/Ishant/design ops/DS-specific shells/TitanEmailShell/`.
Updated registry screen URL, specimen asset URLs and relative stylesheet/inspector
paths for the deeper location. Shared DS content remains at root `design-system/`.

Restored only `Shells/TitanEmailShell/index.html` byte-for-byte from `1712bfe`, the
latest committed version of that file and the pre-DS baseline. Original assets were
unchanged. Historical extraction reports still refer to the original source location;
all ongoing DS-specific development and previews should use the preserved copy.
