# Primary Button

Calendar New Event uses the shared Button with `variant: primary`. The variant changes only the fill and text colors, retaining standard 32px minimum height, typography and spacing. Hover and active use the existing accent overlay tokens; focus and disabled behavior are inherited. Gallery and interaction previews use the shared renderer.
