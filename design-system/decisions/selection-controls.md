# Selection controls

Registered tabs, dropdown trigger and checkbox from the email shell row. Reused
observed 52px tab height, 8px tab insets, 15px text, selected underline, dropdown
6/10px insets, 8px chevron gap, 4px radius and 14px checkbox. Dropdown inset values
remain component-owned. No menu contents or disabled/indeterminate designs invented.

Native buttons/input and keyboard tab navigation provide reusable control behavior.
Shell still uses its original markup here; registration does not claim shell adoption.
Specimens use the shared renderer. All nonzero internal insets/gaps registered;
parent row spacing excluded. Checkbox native checked appearance is browser-owned.
Attachment and component checks run; visual and full browser interaction review pending.
