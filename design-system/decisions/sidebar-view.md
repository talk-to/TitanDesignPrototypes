# Sidebar catalog presentation

The user requested a sidebar view like the supplied reference, retaining Overview.
The shared studio workbench now provides that navigation; this is authorized studio
presentation work rather than app-token customization. App screens and tokens are
unchanged in this task. Catalog contract v1 is preserved.

Overview shows actual declaration/entry counts, not copied adoption percentages.
Foundations has category sub-navigation, resolved-value previews and search. Screens
retains the original previews; components, icons, patterns, directions and moodboard
remain accessible. Existing standalone specimens are retained as source previews,
but the primary browsing entry point is http://localhost:8031/#overview.

Local framework edits are recorded as unreleased. No framework lock or release
version was regenerated. Preserve these changes before a future Git update.
Browser-based visual verification remains unavailable in this session.

Verification: attachment validation passes with zero errors; all 13 studio tests
pass, including foundation category routing and alias/cycle handling. The source
shell hash still matches the completed spacing pass.

User correction: this is a growing Titan DS seeded by one screen, not an email-only
or one-screen system. Rename the catalog to Titan, remove the repeated topbar and
selected-tab heading/description, and let content occupy the full available width
and height. Overview retains its introduction. Sidebar selection and the main
landmark's accessible label identify the selected section.
