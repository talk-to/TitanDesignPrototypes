---
description: Hand a design/ branch to engineering — strip the overlay, collapse variants
argument-hint: [feature|fix|chore]/[name]
---

Invoke the `titan-workflow` skill and follow `handoff.md`, reading `context.md`
alongside it.

Check the audit gate FIRST, before anything else. Do not skip it silently.

Target branch (ask if not given): $ARGUMENTS
