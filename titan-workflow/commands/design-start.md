---
description: Start a design project — cut a design/ branch, launch the server, write the spec
argument-hint: [feature-name]
---

Invoke the `titan-workflow` skill and follow `kickoff.md`, reading `context.md`
alongside it for the memory file format and location.

Feature name (ask if not given): $ARGUMENTS
